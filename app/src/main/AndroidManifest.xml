<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mycarisyours">
    <!-- permission pour acceder a internet -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" /> <!-- les permissions pour la camera et pour le stockage extern -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo_round"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ConfirmerActivity"></activity>
        <activity android:name=".DebutActivity" />
        <activity android:name=".AjouterPhotoActivity" />
        <activity android:name=".AjoutoitureLastActivity" />
        <activity android:name=".AjoutVoitureSuitActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AccueilActivity" />
        <activity android:name=".SigupActivity" />
        <activity android:name=".MapActivity" />
        <activity android:name=".ChoixActivity" />
        <activity android:name=".AjouterVoitureActivity" />
    </application>

</manifest>